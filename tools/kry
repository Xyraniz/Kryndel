#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if [ ! -x "$root/build/kry" ]; then
    printf '%s\n' 'kry: build/kry is missing; run `make build` from the repository checkout' >&2
    exit 69
fi
exec "$root/build/kry" "$@"
