pub fn require(name: String) -> Result[String, String] {
    let value: Option[String] = env_get(name)
    match value {
        some(found) => {
            return ok(found)
        }
        none => {
            return err("required environment variable is missing")
        }
    }
}
